'use client';
import ConstructionPage from '../components/ui/ConstructionPage';

export default function StudioPage() {
    return (
        <ConstructionPage
            title="The Virtual Studio"
            description="Behind the scenes of our 3D avatar rendering. How we achieve photorealistic virtual presence in real-time."
        />
    );
}
