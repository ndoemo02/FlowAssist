'use client';
import ConstructionPage from '../components/ui/ConstructionPage';

export default function CasesPage() {
    return (
        <ConstructionPage
            title="Success Stories"
            description="Real-world results from FlowAssist deployments. 40% reduction in response time across B2B clients."
        />
    );
}
